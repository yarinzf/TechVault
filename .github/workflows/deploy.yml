name: Deploy to Production

on:
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

env:
  SSH_KEY_PATH: /home/runner/.ssh/deploy_key

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Configure SSH
        run: |
          echo "HOME=$HOME"
          pwd
          ls -ld "$HOME"

          if [ -z "${{ secrets.EC2_HOST }}" ]; then echo "::error::EC2_HOST secret is missing"; exit 1; fi
          if [ -z "${{ secrets.EC2_USER }}" ]; then echo "::error::EC2_USER secret is missing"; exit 1; fi
          if [ -z "${{ secrets.EC2_SSH_KEY }}" ]; then echo "::error::EC2_SSH_KEY secret is missing"; exit 1; fi

          mkdir -p "$HOME/.ssh"
          printf '%s\n' "${{ secrets.EC2_SSH_KEY }}" > "$SSH_KEY_PATH"
          chmod 600 "$SSH_KEY_PATH"

          ssh-keyscan -H "${{ secrets.EC2_HOST }}" >> "$HOME/.ssh/known_hosts" 2>/dev/null

          echo "SSH key configured ($(wc -c < "$SSH_KEY_PATH") bytes)"
          echo "known_hosts entries: $(wc -l < "$HOME/.ssh/known_hosts")"

      - name: Sync to origin/main
        run: |
          ssh -i "$SSH_KEY_PATH" -o ConnectTimeout=10 \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} '
            cd /opt/techvault &&
            git fetch origin &&
            git reset --hard origin/main &&
            chmod +x devops/scripts/*.sh
          '

      - name: Pre-deploy MongoDB backup
        run: |
          ssh -i "$SSH_KEY_PATH" -o ConnectTimeout=10 \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} \
            'sudo /opt/techvault/devops/scripts/backup-mongo.sh'

      - name: Build and restart containers
        run: |
          ssh -i "$SSH_KEY_PATH" -o ConnectTimeout=10 \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} '
            cd /opt/techvault &&
            docker compose up -d --build
          '

      - name: Wait for containers to stabilize
        run: sleep 15

      - name: Verify deployment
        run: |
          ssh -i "$SSH_KEY_PATH" -o ConnectTimeout=10 \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} '
            echo "=== Container status ===" &&
            cd /opt/techvault &&
            docker compose ps &&
            echo "" &&
            echo "=== Health endpoint ===" &&
            curl -sf --max-time 10 http://localhost:5000/api/v1/health | python3 -c "
          import sys, json
          d = json.load(sys.stdin)[\"data\"]
          print(\"Status:  \", d[\"status\"])
          print(\"MongoDB: \", d[\"mongodb\"][\"status\"])
          print(\"Version: \", d[\"version\"])
          print(\"Uptime:  \", d[\"uptime\"], \"seconds\")
          if d[\"status\"] != \"healthy\":
              print(\"DEPLOY VERIFICATION FAILED: status is\", d[\"status\"])
              sys.exit(1)
          print(\"Deploy verified: healthy\")
          "
          '

      - name: Post-deploy health check
        run: |
          ssh -i "$SSH_KEY_PATH" -o ConnectTimeout=10 \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} \
            'sudo /opt/techvault/devops/scripts/health-check.sh'

      - name: Cleanup SSH key
        if: always()
        run: rm -f "$SSH_KEY_PATH"
